# Bun and npm configuration
# Note: Bun uses its own lockfile (bun.lock)

# Install behavior
auto-install-peers=true
strict-peer-dependencies=false

# Performance optimizations
side-effects-cache=true

# Allow bun to work properly
engine-strict=false
